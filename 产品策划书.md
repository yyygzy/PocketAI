# PocketAI — 本地优先 AI 工作台 产品策划书

> 版本：v1.0  ｜  定位：可随身携带的全桌面 All-in-One AI Workstation
> 设计参照：Cherry Studio 的导航心智与能力分层，强化「便携 + 加密 + 跨系统」

---

## 一、项目定位

### 1.1 一句话定义
PocketAI 是一款**放在移动硬盘里、插到任意电脑即开即用**的本地优先 AI 工作台——把多模型对话、Agent、知识库、MCP 塞进同一个桌面壳子，模型可替换、数据全留本地、可加密可携带。

### 1.2 核心差异化（与 Cherry Studio 的区别）
| 维度 | Cherry Studio | PocketAI |
|---|---|---|
| 部署形态 | 安装到本机 | **移动硬盘便携，免安装** |
| 跨系统 | 分平台安装包 | **一个文件夹内含多平台运行时，自动适配** |
| 数据位置 | `%APPDATA%` 等系统目录 | **应用同级 `data/`，跟着硬盘走** |
| 加密 | 基础 | **三层加密（卷/库/字段），用户可选粒度** |
| 工具执行 | 含终端/沙箱 | v1 仅联网/文件/日历，**终端与沙箱延后** |

### 1.3 设计原则
- **默认克制，处处留螺丝刀**：开箱够用，高级用户可深度定制。
- **模型是后端，不是产品**：统一抽象层，不绑定任何厂商。
- **数据跟人走**：硬盘到哪，会话/助手/知识库/密钥到哪。
- **安全可配置**：不强制加密，但提供从「完全不加密」到「全盘加密」的分级选项。

---

## 二、可行性分析（诚实评估）

### 2.1 最大技术挑战：真正的「跨系统便携」

**结论：单二进制跨操作系统不可能，但「一个文件夹内含多平台运行时」完全可行，且是业界成熟做法。**

#### 为什么单包跨 OS 不可能
- Electron 运行时（Chromium + Node）是**平台原生二进制**，Windows 是 `.exe`，macOS 是 `.app`，Linux 是 ELF，互不通用。
- 原生 Node 模块（`better-sqlite3`、`node-pty` 等）是**按平台+架构编译**的 `.node` 文件。
- 因此不存在「一个 exe 插到 Mac 也能跑」。

#### 可行方案：多运行时同包 + 共享数据
移动硬盘里放**一个文件夹**，内含所有平台的运行时与一份共享数据。用户插到任意机器，运行对应启动器，数据层指向同一份 `data/`。

```
PocketAI/                      ← 整个文件夹放在移动硬盘
├── runtime/                   ← 各平台运行时（全部预装）
│   ├── win-x64/
│   ├── darwin-x64/
│   ├── darwin-arm64/
│   └── linux-x64/
├── data/                      ← ★ 共享数据（跟着硬盘走）
│   ├── app.db                 ← SQLCipher 加密
│   ├── vectors.db             ← 向量库
│   ├── attachments/
│   └── config.json
├── extensions/                ← MCP 服务 / Skills（JS 跨平台）
├── start.bat                  ← Windows 启动
├── start.sh                   ← macOS/Linux 启动
└── PocketAI.app / PocketAI.exe ← 启动入口（按平台存在）
```

**体积评估**：单平台 Electron 运行时约 150–200MB，四平台全装约 700MB–1GB。对移动硬盘完全可接受。

#### 关键工程约束（来自过往经验）
1. **数据路径必须锚定应用目录**，绝对不能用 `app.getPath('userData')`（那会写到系统用户目录，破坏便携）。应以 `process.resourcesPath` 或可执行文件位置为基准，向上定位 `data/`。
2. **启动器与 cwd 解耦**：所有内部路径用「应用目录锚定的绝对路径」，不依赖当前工作目录，否则换个启动方式就读不到数据。
3. **原生模块动态加载**：`better-sqlite3` 等需为每个平台预编译 `.node`，运行时按 `process.platform + process.arch` 选择加载。

### 2.2 加密可行性
- **卷级**：依赖 VeraCrypt 便携版或 BitLocker，外层防护，丢盘即不可读。可行。
- **库级**：`@journeyapps/sqlcipher`（SQLCipher 的 Node 绑定，已预编译多平台）。可行。
- **字段级**：API Key 等敏感字段用 Argon2id 派生密钥 + AES-256-GCM 加密。可行。
- **跨机密钥一致性**：因不能依赖 OS Keychain（跨机失效），统一用「用户主密码派生密钥」。可行。

### 2.3 MCP 可移植性
- **Node 系 MCP Server**：Electron 自带 Node 运行时，可直接 spawn，零外部依赖。✅ 可移植。
- **Python 系 MCP Server**：需捆绑便携 Python（约 50MB+，且分平台）。v1 建议**仅支持 Node 系 MCP**，Python 系作为 v2 可选下载项。
- **协议**：本地用 stdio（无端口冲突、生命周期干净）；远端用 HTTP/SSE。

### 2.4 知识库与向量
- `better-sqlite3` + `sqlite-vec`（SQLite 向量扩展，分平台 `.dll/.so/.dylib`，全平台预编译随包）。✅ 可行。
- 文档解析：`pdf-parse`、`mammoth`（docx）、`xlsx`、`cheerio`（html）、`sitemapper`，均为纯 JS。✅ 可移植。
- Embedding：优先走用户配置的模型 API（OpenAI/Ollama 本地 embedding），不强绑。

### 2.5 综合可行性结论
| 模块 | 可行性 | 备注 |
|---|---|---|
| 多运行时同包便携 | ✅ 高 | 核心策略，业界成熟 |
| 三层加密 | ✅ 高 | SQLCipher + Argon2id |
| Provider 抽象 | ✅ 高 | 成熟模式 |
| 多模型并行对照 | ✅ 高 | 并发请求 + 流式合并 |
| MCP（Node 系） | ✅ 高 | Electron 自带 Node |
| MCP（Python 系） | ⚠️ 中 | v2，需捆绑便携 Python |
| 终端/沙箱执行 | ⚠️ 中 | 用户已明确 v1 不做 |
| 跨窗口状态共享 | ✅ 高 | 主进程单内核 |
| WebDAV 备份 | ✅ 高 | webdav 库 |

---

## 三、核心设计决策

### 3.1 便携策略（最关键决策）

**「一个文件夹，多运行时，共享数据」**，而非追求单二进制。

- 交付形态：一个 `PocketAI` 文件夹，内含 `runtime/`（多平台 Electron）+ `data/`（共享）+ `extensions/`。
- 启动：用户双击对应平台的启动器（或一个智能启动脚本自动检测平台）。
- 数据：`data/` 永远在应用同级，随硬盘迁移。
- 路径锚定：所有数据访问以「可执行文件所在目录」为基准，不依赖 cwd、不依赖系统目录。

> 经验依据：后台/不同启动器会改变 cwd 与可执行文件解析，相对路径在这些环境下不稳定，必须用「应用目录锚定的绝对路径」消除不确定性。

### 3.2 加密分层策略

| 层级 | 保护对象 | 技术 | 用户可选 |
|---|---|---|---|
| L1 卷级 | 整个硬盘/分区 | VeraCrypt 便携版 / BitLocker | 是（默认关） |
| L2 库级 | SQLite 数据库文件 | SQLCipher（AES-256-CBC） | 是（建议开） |
| L3 字段级 | API Key、隐私会话内容 | Argon2id 派生密钥 + AES-256-GCM | 是（随库级联动） |

**密钥管理**：
- 用户设置主密码 → `Argon2id(password, salt)` 派生 256-bit 主密钥。
- 主密钥 → SQLCipher key + 字段加密 key（通过 HKDF 拆分）。
- `salt` 存于 `data/config.json`（明文，salt 不保密）。
- 可选「无密码模式」：主密钥用固定默认值（仅用于无敏感数据场景，UI 明确警告）。
- **不依赖 OS Keychain**，保证跨机一致。

### 3.3 MCP 运行时策略
- v1：**仅支持 Node.js 系 MCP Server**（stdio 传输），利用 Electron 内置 Node 直接 spawn，零外部依赖。
- v2：支持 Python 系，可选下载便携 Python 运行时到 `runtime/python-{platform}/`。
- MCP Server 的安装与启停由 App 托管，配置存于本地库。

### 3.4 工具调用边界（v1）
用户已明确：v1 不做终端命令执行与沙箱。仅提供轻量工具：
- **联网搜索**（fetch + 搜索引擎 API / SerpAPI / 自建）
- **文件读写**（限定在用户授权的目录，App 内文件管理）
- **日历**（读本地 `.ics` / 飞书日历 API，只读为主）
- **网页抓取**（fetch + readability 提取正文）
- **计算器 / 时间 / 天气** 等无副作用工具

> 沙箱与命令执行放到 v2，避免 v1 陷入安全合规泥潭。

---

## 四、信息架构与交互

### 4.1 三层导航
```
┌─────────────────────────────────────────────┐
│ Sidebar │  Tab Bar (会话标签)                │
│         ├─────────────────────────────────── │
│ 对话     │                                   │
│ Agent    │      主工作区（聊天/Agent/...）    │
│ 知识库    │                                   │
│ 文件     │                                   │
│ 设置     │                                   │
└─────────┴───────────────────────────────────┘
   浮窗层：快捷问答(全局快捷键) / 选区助手(划词) / 剥离标签子窗口
```

| 层级 | 角色 | 承载 |
|---|---|---|
| 左侧 Sidebar | 工作区切换器（可折叠/悬浮/宽度持久化） | 对话 / Agent / 知识库 / 文件 / 设置 |
| 顶部 Tab Bar | 浏览器式会话标签 | 多并发会话、拖拽排序、固定、剥离成独立窗口 |
| 浮层窗口 | 脱离主窗口的轻量入口 | 快捷问答 / 选区助手 / detached tab |

**职责分离**：侧栏切模块，标签切实例。历史会话不与工具面板混在一起。

### 4.2 标签生命周期状态机
```
active → pinned → dormant(休眠, 释放渲染) → detached(独立窗口) → closed
```
- 固定标签：不被自动关闭。
- 休眠态：超 N 个标签后自动休眠，释放内存，保留滚动位置。
- 剥离：detached tab 变成独立 `BrowserWindow`，与主窗口共享内核。

### 4.3 对话主区交互
1. **一问多答（Multi-model parallel）**：同一输入并发发往 N 个模型，左右/上下对照，「选模型」变「对比后挑」。
2. **按助手归集会话**：每个 Assistant/Agent 的聊天记录独立成组，不做全局扁平流。
3. **富消息渲染**：Markdown / LaTeX / Mermaid / HTML 预览 / 代码高亮 / 引用切片，一等公民。
4. **多模态输入框**：附件、@知识库、@工具、参数覆盖、临时 system 改写，统一从输入条展开。
5. **可编辑消息**：停止 / 重生成 / 分支 / 改参重跑，每条 AI 消息可回退。

### 4.4 会话消息状态机
```
idle → streaming → paused → done / error / aborted
```
每条消息带可追溯状态位，支持断点续流（网络恢复后继续）。

---

## 五、能力分层架构

```
┌─ L5 表现层      侧栏 / 标签 / 气泡 / 浮窗 / Custom CSS
├─ L4 能力插件层   MCP Server · Skills · Mini Program · 定时任务 · Channels
├─ L3 上下文层     助手 SystemPrompt · 知识库 RAG 注入 · 工具 schema · 记忆/变量
├─ L2 模型抽象层   Provider Adapter → 统一 ChatCompletion
└─ L1 本地数据层   SQLCipher + sqlite-vec + WebDAV 备份 + 本地 OCR
```

### L1 本地数据层
- `better-sqlite3` + `@journeyapps/sqlcipher`（加密 SQLite）
- `sqlite-vec` 向量扩展（全平台预编译随包）
- WebDAV 备份（`webdav` 库）
- 纯 JS 文档解析（pdf/docx/xlsx/html/sitemap）

### L2 模型抽象层
统一 `ChatCompletion` 接口，适配器消化差异：
- **Provider 适配**：OpenAI / Gemini / Anthropic / Azure / Ollama / LM Studio / 任意 OpenAI-compatible
- **归一化能力**：多 Key 轮询、模型列表自动拉取、头像/品牌色映射、流式 SSE 统一
- **上层零感知**：业务层只调 `provider.chat(messages, options)`

### L3 上下文层
- 助手 SystemPrompt（模板 + 变量占位）
- 知识库 RAG 注入（检索 → 重排 → 拼装到 context）
- 工具 schema 注入（MCP tools + 内置 tools）
- 记忆/变量（用户级、会话级、助手级）

### L4 能力插件层
- MCP Server 管理（安装/启停/权限）
- **Skills（可复用能力包）**：如「小红书图文」「流程图」「SQL 优化」，本质是提示模板+工具权限声明+输出格式，纯数据无代码，天然安全
- Mini Program（v2）
- Channels（飞书/TG/Discord，v2）
- 定时任务（v2）

**Skill 与 Assistant 的关系**：Skill 是插件式能力包，Assistant 是容器；一个 Assistant 可挂载 0~N 个 Skill。

**Skill 分发策略（分阶段）**：
- **v1**：内置 6–10 个 Skill，随包放在 `extensions/skills/`，用户可开关/排序、在助手详情页勾选挂载。**不设独立市场**。
- **v2**：合并入统一「能力市场（Capabilities Market）」，与 Assistant、MCP Server 共享分发框架，用 `type: assistant|skill|mcp` 标签区分，避免用户面对两个市场心智混乱。
- **包格式**：`skill.json`（元数据）+ `prompt.md`（模板）+ `tools.json`（工具权限声明）+ `examples/`；支持内置源 + 自定义源（GitHub Raw / WebDAV / 本地文件夹，贴合本地优先）。

### L5 表现层
- 侧栏 / 标签 / 气泡 / 浮窗
- Custom CSS 注入
- 明/暗/跟随系统 + 透明窗口
- 气泡/列表双布局

---

## 六、功能模块规划（v1 范围）

### 6.1 必做模块（v1）

| 模块 | 子功能 |
|---|---|
| **模型管理** | Provider 增删改、多 Key 轮询、模型列表拉取、头像映射 |
| **多模型对话** | 一问多答并发、流式渲染、对照视图 |
| **助手市场** | 内置 8 个业务助手 + 1 个平台管家，可安装/卸载/置顶 |
| **助手会话** | 按助手归集、分组、搜索 |
| **消息编辑** | 停止/重生成/分支/改参重跑 |
| **知识库** | PDF/DOCX/PPTX/XLSX/URL/Sitemap/手工录入、chunk 预览、检索测试、RAG 注入 |
| **MCP** | Node 系 Server 安装/启停/工具调用 |
| **Work Agent** | 带工具权限的 Agent（联网/文件/日历/抓取），多步推理 |
| **平台管家** | 硬件检测/模型推荐下载/安全检测/垃圾清理/故障诊断与修复（内置系统工具） |
| **三层导航** | Sidebar + Tab + 浮窗 |
| **快捷问答** | 全局快捷键唤起浮窗 |
| **选区助手** | 划词浮条（翻译/总结/改写） |
| **三层加密** | 卷/库/字段，用户可选 |
| **界面隐私保护** | 手动/自动/系统联动锁屏，后台任务不中断，防窥（见 10.5） |
| **WebDAV 备份** | 手动/定时备份到 WebDAV |
| **便携启动** | 多运行时同包、数据路径锚定 |
| **定制** | 主题/透明/Custom CSS/快捷键/侧栏排序 |

### 6.2 延后模块（v2+）
绘图 / 翻译（独立模块）/ 迷你应用 / 笔记 / 文件管理（独立模块）/ 终端命令 / 沙箱 / Python MCP / Channels

### 6.3 内置助手市场（v1 首批 9 个）

> 分两类：**业务助手**（面向内容生产）+ **平台管家助手**（面向系统运维，因便携+小白场景而设，属于特殊内置助手，拥有系统级工具权限）。

#### 业务助手（8 个）
1. **通用问答助手**（默认，多模型对照）
2. **代码工程师**（代码生成/审查/解释，内置代码工具）
3. **写作润色**（长文结构/风格统一/语法）
4. **学术助手**（文献综述/公式推导/引用格式）
5. **数据分析师**（表格解读/SQL/可视化建议）
6. **产品经理**（PRD/需求拆解/竞品分析）
7. **翻译专家**（多语种，保留术语表）
8. **学习教练**（费曼技巧/知识卡片/测验生成）

#### 平台管家助手（1 个，特殊内置）
9. **平台管家（Platform Steward）**——见 6.4 专章

> 每个业务助手 = SystemPrompt + 默认模型 + 工具权限集 + 欢迎语 + 头像。纯数据，可热更新。

### 6.4 平台管家助手（Platform Steward）—— 便携工作台的「随车维修工」

#### 为什么需要它
PocketAI 的核心场景是**移动硬盘 + 频繁换电脑 + 大量小白用户**，这带来三个痛点：
- 每台电脑硬件不同（CPU/RAM/GPU），用户不知道该下哪个本地模型
- 换电脑后路径、权限、Provider 连接可能出问题，小白不会排查
- 长期使用产生缓存/日志/孤儿附件，拖慢便携硬盘

平台管家是一个**拥有系统级工具权限的特殊内置助手**，负责硬件检测、模型推荐下载、安全巡检、垃圾清理、故障诊断与修复。它不是普通业务助手，而是工作台的「健康中心」。

#### 五大能力域

##### 1. 硬件检测
- 采集：CPU 型号/核数、内存总量与可用、GPU（显存/是否支持 CUDA/MPS）、磁盘类型（SSD/HDD/USB 版本）、操作系统与版本
- 输出：一份「本机能力画像」，给出性能档位（入门/主流/高性能）
- 用途：作为模型推荐的依据，也存入会话上下文供后续决策

##### 2. 模型推荐与下载
- 根据硬件画像推荐 Ollama / LM Studio 本地模型（如：8GB 内存 → 7B Q4；16GB+ → 13B Q4；有独显 → 推荐 GPU 加速）
- 一键下载/卸载模型，显示进度与剩余空间
- 模型列表本地缓存，支持离线查看已下载模型
- 自动判断 Ollama/LM Studio 是否已安装，未装则引导（v1 可只做检测+推荐，下载走官方安装器；v2 可捆绑便携 Ollama）

##### 3. 安全检测
- 加密级别检查：当前是 none/db/field/volume 哪一级，给出风险提示
- API Key 完整性检查：是否有 Key 过期/失效、是否明文存储
- 端口与进程检查：是否有 MCP Server 异常占用端口、Provider 连接是否泄漏
- 权限检查：数据目录读写权限是否正常
- 输出「安全体检报告」+ 一键修复建议

##### 4. 平台垃圾清理
- 清理项（均需用户确认）：
  - 临时文件 / 日志（`data/logs/`）
  - 已删除会话的孤儿附件
  - 向量库中已删除文档的残留 chunk
  - Provider 模型列表缓存（可重新拉取）
  - 失败的 MCP 安装残留
- 提供「安全清理」（只清确定无用的）与「深度清理」（需二次确认）两档
- 清理前显示可释放空间预估

##### 5. 故障诊断与修复
- **诊断项**：
  - 数据库完整性（SQLCipher 是否能正常打开、表结构是否完整）
  - Provider 连通性（逐个 ping 各 Provider base_url + Key 有效性）
  - MCP Server 健康（是否能正常 spawn、握手是否成功）
  - 路径锚定（data 目录是否在应用同级、读写权限）
  - 向量索引一致性（chunk 数与文档数是否匹配）
- **修复策略**：
  - 自动修复（无需确认）：重建索引、清理临时文件、重置连接
  - 半自动修复（需确认）：重置某个 Provider、重启某个 MCP
  - 不可自动修复：生成诊断报告，引导用户操作或导出日志给开发者
- 诊断报告可导出为文本，便于远程求助

#### 平台管家的工具集（内置系统工具，不走 MCP）

| 工具 | 说明 |
|---|---|
| `system.hardware_info` | 返回 CPU/RAM/GPU/磁盘/OS 画像 |
| `model.local.list` | 列出已安装的本地模型（Ollama/LM Studio） |
| `model.local.recommend` | 根据硬件返回推荐模型清单 |
| `model.local.pull` | 下载指定模型（异步，返回进度） |
| `security.audit` | 跑安全检测，返回报告 |
| `system.cleanup` | 执行垃圾清理，返回释放空间 |
| `system.diagnose` | 跑全量诊断，返回问题清单 |
| `system.repair` | 对指定问题执行修复（需确认） |

> 这些是**内置工具**，直接在主进程实现，不经 MCP，确保权限可控、不依赖外部服务。

#### 交互形态
- **入口**：侧栏独立图标（健康中心）+ 首次启动自动跑一次「快速体检」+ 设置页入口
- **首次启动引导**：检测硬件 → 推荐模型 → 检查加密 → 给出「开始使用」或「先优化」建议
- **换电脑提示**：检测到运行在新机器上（硬件指纹变化），自动提示「检测到新设备，是否运行平台管家体检？」
- **持续守护**：可选「后台定时巡检」（默认关，避免打扰），发现严重问题（如数据库异常）主动弹窗

#### 与普通助手的区别
| | 业务助手 | 平台管家 |
|---|---|---|
| 权限 | 业务工具（联网/文件/日历） | 系统级工具（硬件/进程/数据库） |
| 数据 | 可挂载 Skill | 不可挂载业务 Skill（防止越权） |
| 会话归集 | 按助手独立分组 | 独立分组，且会话默认不进入全局搜索 |
| 删除 | 可卸载 | 不可卸载，永久内置 |

---

## 七、技术选型

### 7.1 基础栈
| 层 | 选型 | 理由 |
|---|---|---|
| 桌面框架 | **Electron 30+** | 成熟、跨平台、生态全 |
| 前端 | **React 18 + TypeScript + Vite** | 社区大、工具链成熟 |
| 状态管理 | **Zustand**（轻量）+ 主进程共享 Store | 多窗口共享需主进程为真源 |
| UI 组件 | **shadcn/ui** 或自研 + Tailwind | 克制、可定制 |
| 富文本渲染 | **react-markdown + remark-math + rehype-highlight + mermaid** | 一等公民渲染 |
| 数据库 | **better-sqlite3 + @journeyapps/sqlcipher** | 同步 API、性能好、加密 |
| 向量 | **sqlite-vec** | SQLite 原生向量扩展，零外部依赖 |
| 通信 | **Electron IPC + contextBridge** | 安全模型：nodeIntegration=false, contextIsolation=true |
| WebDAV | **webdav** 库 | 成熟 |
| 加密 | **Argon2 (hash-wasm) + crypto (Node built-in)** | Argon2id 抗 GPU，AES-256-GCM |

### 7.2 安全模型（硬约束）
- `nodeIntegration: false`，`contextIsolation: true`，`sandbox: true`（除非确需关闭）
- 所有 Node/文件/进程能力通过 `preload` 暴露白名单 API
- renderer 内**禁止**直接用 `process`/`fs`/`path`
- 经验教训：renderer 用 `process.cwd()` 等 Node API 会与 sandbox 冲突，必须走 preload

### 7.3 工程结构
```
src/
├── main/                    # 主进程
│   ├── index.ts             # 入口，创建窗口
│   ├── portable.ts          # 路径锚定、平台检测
│   ├── db/                  # SQLCipher 连接、迁移
│   ├── providers/           # L2 模型抽象层
│   ├── mcp/                 # MCP server 生命周期
│   ├── agent/               # Work Agent 引擎
│   ├── knowledge/           # 知识库 + 向量
│   ├── crypto/              # 加密层
│   ├── backup/              # WebDAV
│   └── ipc/                 # IPC 路由
├── renderer/                # 渲染进程
│   ├── App.tsx
│   ├── layout/              # Sidebar / TabBar / FloatingWindow
│   ├── modules/             # chat / agent / knowledge / settings
│   └── components/
├── preload/                 # 安全桥
└── shared/                  # 类型、常量
```

---

## 八、数据模型（核心表）

```sql
-- 助手
CREATE TABLE assistants (
  id TEXT PRIMARY KEY,
  name TEXT, avatar TEXT, system_prompt TEXT,
  default_provider TEXT, default_model TEXT,
  tool_permissions TEXT,   -- JSON 数组
  welcome TEXT,
  is_builtin INTEGER, pinned INTEGER, created_at INTEGER
);

-- 会话（按助手归集）
CREATE TABLE conversations (
  id TEXT PRIMARY KEY,
  assistant_id TEXT REFERENCES assistants(id),
  title TEXT, model TEXT, params TEXT,  -- JSON
  status TEXT,            -- idle/streaming/done/error
  created_at INTEGER, updated_at INTEGER
);

-- 消息
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  conversation_id TEXT REFERENCES conversations(id),
  role TEXT,             -- user/assistant/system/tool
  content TEXT,          -- 富文本/JSON
  provider TEXT, model TEXT,
  status TEXT,           -- streaming/done/error/aborted
  parent_id TEXT,        -- 分支支持
  created_at INTEGER
);

-- Provider / Key
CREATE TABLE providers (
  id TEXT PRIMARY KEY,
  type TEXT,             -- openai/gemini/...
  name TEXT, base_url TEXT,
  api_key_encrypted TEXT,-- 字段级加密
  models TEXT            -- JSON 缓存
);

-- 知识库
CREATE TABLE knowledge_bases (
  id TEXT PRIMARY KEY, name TEXT,
  embedding_provider TEXT, embedding_model TEXT,
  chunk_size INTEGER, chunk_overlap INTEGER
);
CREATE TABLE kb_documents (
  id TEXT PRIMARY KEY, kb_id TEXT, source TEXT,
  source_type TEXT,     -- pdf/docx/url/...
  chunk_count INTEGER, status TEXT
);
CREATE TABLE kb_chunks (
  id TEXT PRIMARY KEY, doc_id TEXT,
  content TEXT, embedding BLOB   -- sqlite-vec
);

-- MCP Server
CREATE TABLE mcp_servers (
  id TEXT PRIMARY KEY,
  name TEXT, transport TEXT,     -- stdio/http
  command TEXT, args TEXT,       -- stdio
  url TEXT,                      -- http
  enabled INTEGER
);

-- 密钥派生 salt（明文）
-- config.json: { "kdf_salt": "hex...", "encryption_level": "none|db|field|volume" }
```

---

## 九、可移植性工程细节

### 9.1 路径锚定规则
```ts
// main/portable.ts
import { app } from 'electron'
import path from 'node:path'
import fs from 'node:fs'

// 可执行文件所在目录（打包后）或项目根（开发时）
const APP_ROOT = app.isPackaged
  ? path.dirname(app.getPath('exe'))
  : process.cwd()

// 数据目录 = APP_ROOT/data（永远跟着应用走）
export const DATA_DIR = path.join(APP_ROOT, 'data')
export const DB_PATH = path.join(DATA_DIR, 'app.db')
export const VECTOR_DB_PATH = path.join(DATA_DIR, 'vectors.db')
export const CONFIG_PATH = path.join(DATA_DIR, 'config.json')
export const ATTACHMENTS_DIR = path.join(DATA_DIR, 'attachments')
export const EXTENSIONS_DIR = path.join(APP_ROOT, 'extensions')

// 确保目录存在
export function ensureDirs() {
  ;[DATA_DIR, ATTACHMENTS_DIR, EXTENSIONS_DIR].forEach(d =>
    fs.mkdirSync(d, { recursive: true })
  )
}
```

> **绝对禁止**使用 `app.getPath('userData')` / `app.getPath('appData')`，那会写到系统目录，破坏便携。

### 9.2 原生模块多平台加载
`better-sqlite3`、`sqlite-vec` 需为每个平台预编译。构建时产出：
```
node_modules/better-sqlite3/build/Release/
  win-x64/better_sqlite3.node
  darwin-x64/better_sqlite3.node
  darwin-arm64/better_sqlite3.node
  linux-x64/better_sqlite3.node
```
运行时：
```ts
const nativeName = `${process.platform}-${process.arch}`
const nodePath = path.join(__dirname, `native/${nativeName}/better_sqlite3.node`)
const Database = require(nodePath)
```

### 9.3 启动器
提供 `start.bat`（Windows）与 `start.sh`（macOS/Linux），或一个智能入口：
- Windows：`PocketAI.exe`（即 Electron 的 win 构建）
- macOS：`PocketAI.app`
- Linux：`PocketAI` ELF

用户只需双击自己平台的入口，App 自动定位同级 `data/`。

---

## 十、加密设计详解

### 10.1 加密技术栈与语言
全部使用 **TypeScript**（主进程 + 渲染进程统一），加密原语选型：

| 用途 | 库 | 选型理由 |
|---|---|---|
| 密钥派生（主密码 → 密钥） | **hash-wasm**（Argon2id） | WASM 实现，无原生编译，跨平台零差异，抗 GPU 暴力破解 |
| 字段加密（API Key 等） | Node 内置 `crypto`（AES-256-GCM） | 主进程原生可用，性能好，GCM 自带完整性校验 |
| 数据库加密 | `@journeyapps/sqlcipher` | SQLCipher 的 Node 绑定，多平台预编译随包，AES-256-CBC 加密整个 SQLite |
| 授权签名校验 | Node 内置 `crypto`（RSA-SHA256） | 离线验证授权文件，见第十三章 |

> **硬约束**：所有加密运算跑在**主进程**，渲染进程沙箱化不碰密钥；密钥仅存内存，永不落盘。

### 10.2 密钥派生架构
```
用户输入主密码
      │
      ▼
┌─────────────────────────────┐
│  主进程 crypto 模块           │
│  Argon2id(password, salt)    │  ← salt 存 data/config.json（salt 不保密）
│  m=64MB, t=3, p=4            │
│  → 256-bit master_key        │
└──────────┬──────────────────┘
           │ HKDF-SHA256 拆分
     ┌─────┴──────┐
     ▼            ▼
 sqlcipher_key   field_key
     │            │
     ▼            ▼
 打开加密 SQLite   AES-256-GCM 加解密
 (app.db 整体加密)  (providers.api_key 等字段)
```

### 10.3 加密级别（用户可选）
| 级别 | 效果 | 适用场景 |
|---|---|---|
| `none` | 不加密，明文 SQLite | 纯本地、无敏感数据（UI 红色警告） |
| `db` | SQLCipher 加密整个数据库 | **推荐默认** |
| `field` | db 级 + API Key 等字段额外加密 | 多密钥、高敏感场景 |
| `volume` | 提示用户用 VeraCrypt/BitLocker 加密整个卷 | 最高安全（丢盘不可读） |

**无密码模式**：主密钥使用固定默认值，仅用于无敏感数据场景，首次设置时明确警告。

### 10.4 运行时内存安全
- 主密钥仅在内存中持有，**锁屏/超时自动清除**（清除后需重新输入密码解锁）。
- 剪贴板写入可选自动清除（复制 API Key 等敏感内容 N 秒后清空）。
- 日志中**绝不**打印密钥、完整对话、授权签名。
- 主密码恢复：设置时可导出「恢复密钥」文件（存于别处），或设置密保问题（小白友好但安全性略低）。

### 10.5 界面隐私保护（隐私锁）
针对「用户临时离开、任务仍在运行、怕被他人看见」的场景：

| 触发方式 | 行为 |
|---|---|
| **手动锁屏** | 快捷键 / 托盘菜单 / 窗口失焦超时 |
| **自动锁屏** | 空闲 N 秒后（用户可配置，默认 5 分钟） |
| **系统联动** | 操作系统锁屏时同步锁定 PocketAI |

**锁屏后的表现**：
- 窗口内容被模糊/遮罩覆盖，不显示任何会话、密钥、知识库内容
- **后台任务不中断**：正在进行的对话流式输出、模型下载、知识库向量化、WebDAV 备份继续运行
- 解锁需输入主密码（若开启了加密）或独立的「锁屏密码」（与主密码可不同）
- 解锁后恢复到离开时的界面状态

**隐私锁与主密码的关系**：
- 若开启了 `db`/`field` 加密，隐私锁复用主密码
- 若选 `none` 加密，用户仍可单独设置锁屏密码（不加密数据，但防窥）
- 锁屏仅隐藏界面，不解密/关闭数据库（保证后台任务继续）

**实现要点**：
- 锁屏状态由主进程 Store 持有，多窗口同步
- 锁屏遮罩渲染在所有窗口之上（BrowserWindow 顶层或 CSS 全屏遮罩）
- 后台任务的 IPC 通信不受锁屏影响，但 UI 刷新被遮罩遮挡

---

## 十一、多窗口与状态共享

- **主进程 = 唯一真源**：Provider 缓存、KB 索引、MCP 连接池都在主进程。
- **多窗口 = 多 Renderer**：每个 Tab/浮窗是独立 `BrowserWindow` 或同一窗口的视图，通过 IPC 向主进程请求数据。
- **共享内核**：同一 Provider 实例、同一 DB 连接、同一向量索引，避免重复加载。
- **UI 状态隔离**：每个窗口的滚动、输入框内容独立；后端状态共享。

---

## 十二、配置优先级（热覆盖）
```
单轮临时参数（输入框展开）  >  助手级参数  >  全局参数
```
越靠近输入框权重越高。每次请求前做一次 merge。

---

## 十三、商用授权方案（买断制）

### 13.1 授权模式选择
采用**离线签名授权文件**模式，核心原因：
- 符合本地优先哲学——核心功能完全离线可用
- 适配便携场景——授权文件跟着移动硬盘走，换电脑无需重新激活
- 你完全掌控——私钥在你手中，可签发/吊销

### 13.2 授权流程
```
你的服务器（持 RSA 私钥）        用户的 PocketAI（内置 RSA 公钥）
      │                                │
      │ ① 用户付款（买断）              │
      │ ② 生成授权载荷 JSON             │
      │ ③ RSA-SHA256 签名               │
      │ ④ 输出 license.lic 文件         │
      ├─────── 发给用户 ───────────────▶│
      │                                │ ⑤ App 用内置公钥验签
      │                                │ ⑥ 验签通过 → 解锁对应功能
      │                                │ ⑦ 授权存入 data/license.lic
```

### 13.3 授权载荷格式
```json
{
  "licensee": "user@example.com",
  "plan": "pro",
  "issued_at": 1694900000,
  "expires_at": 0,
  "update_until": 1726436000,
  "max_devices": 5,
  "features": ["mcp", "knowledge_base", "platform_steward", "webdav", "multi_model_compare"],
  "signature": "Base64(RSA-SHA256(payload_without_signature))"
}
```
- `expires_at: 0` 表示永久授权（买断）
- `update_until` 表示免费更新截止时间（买断含 1 年更新，过期后仍可使用但不升级）
- `max_devices` 限制同时激活的设备数（授权文件可复制，但超出设备数时联网校验）

### 13.4 版本与定价（建议）

| 版本 | 价格 | 功能 |
|---|---|---|
| **免费版** | ¥0 | 2 个助手、2 个 Provider、基础对话、无知识库/MCP |
| **专业版（买断）** | ¥199 | 全部功能、无限助手/Provider、知识库、MCP、平台管家、WebDAV、1 年免费更新 |
| **专业版更新续费** | ¥79/年 | 买断到期后续费 1 年更新权限 |
| **团队版** | ¥299/席位 | 专业版 + 团队授权 + 共享知识库模板 + 优先支持 |

> 买断制用户心理：一次付费永久使用，更新单独续费——桌面软件主流模式，接受度高。

### 13.5 防破解分层
| 层级 | 手段 | 说明 |
|---|---|---|
| L1 签名校验 | RSA 公钥内置，启动 + 调用付费功能时实时验签 | 改授权文件签名无效 |
| L2 代码保护 | `javascript-obfuscator` 混淆主进程授权校验代码 | 增加逆向成本 |
| L3 原生校验 | 授权核心逻辑编译为 `.node` 原生模块 | 比纯 JS 难逆向 |
| L4 时间防回拨 | 授权带 `issued_at`，系统时间早于签发时间触发告警 | 防止改系统时间绕过更新过期 |
| L5 联网可选校验 | 超 `max_devices` 时触发联网校验（非强制） | 团队版/滥用检测 |

> 目标：让破解成本 > 购买成本，不追求 100% 不可破解。

### 13.6 交付物
- **服务端**：Node 签发脚本（`node:crypto` RSA 签名），可对接支付自动发货
- **客户端**：主进程内置 RSA 公钥，启动时读 `data/license.lic` 验签
- **用户**：收到 `license.lic` 文件，放入 `data/` 目录或在 App 内「导入授权」

---

## 十四、更新机制

### 14.1 便携场景的更新痛点
传统 Electron 自动更新（electron-updater）会下载完整安装包，但便携场景下：
- 文件夹约 1GB，全量更新太慢
- 用户在不同电脑间切换，更新要跟着硬盘走
- 不能依赖系统级安装器

### 14.2 增量更新方案（推荐）
```
PocketAI/
├── runtime/
│   └── win-x64/
│       ├── app.asar        ← 应用代码（可单独替换）
│       └── ...             ← Electron 运行时（很少变）
├── data/                   ← 用户数据（永不覆盖）
└── extensions/             ← 用户扩展（永不覆盖）
```

**更新只替换 `app.asar`**（应用代码，通常 10–30MB），运行时和数据不动：
- 用户下载 `app.asar.patch`（差量包，可能更小）或新版 `app.asar`
- 放入指定目录，App 检测到后提示「重启以应用更新」
- 重启时原子替换，失败自动回滚

### 14.3 更新分发渠道
| 渠道 | 说明 |
|---|---|
| **官方更新源** | 你维护的 WebDAV / 静态文件服务器，App 内一键检查更新 |
| **手动更新** | 用户下载新版 `app.asar` 拖入 App，自动校验签名后替换 |
| **全量包更新** | 大版本（运行时升级）提供完整便携包下载 |

### 14.4 更新安全
- 更新包必须用你的 RSA 私钥签名，App 内置公钥验签
- 验签失败拒绝更新，防止篡改
- 更新前自动备份当前 `app.asar`，更新失败自动回滚
- 数据目录 `data/` 在更新中**只读不写**，绝不覆盖用户数据

### 14.5 版本与更新策略
- **小版本（x.y.z → x.y.z+1）**：仅 `app.asar` 增量更新，免费
- **中版本（x.y → x.y+1）**：`app.asar` 更新，可能含运行时补丁，免费
- **大版本（x → x+1）**：可能需更新 Electron 运行时，提供全量包；买断用户在 `update_until` 内免费

---

## 十五、开发路线图

### Phase 0：脚手架与便携壳（2–3 周）
- [ ] Electron + React + TS + Vite 工程
- [ ] 路径锚定、多平台原生模块加载
- [ ] 三层导航骨架（Sidebar/Tab/浮窗）
- [ ] SQLCipher 连接 + 迁移框架
- [ ] 主进程 Store + IPC 桥
- [ ] **平台管家地基：`system.hardware_info` 工具（CPU/RAM/GPU/磁盘/OS 采集）**
- **交付**：一个能在多平台启动、共享 `data/` 的空壳

### Phase 1：核心对话（3–4 周）
- [ ] Provider 抽象层（OpenAI/Ollama/兼容端点优先）
- [ ] 单模型聊天 + 流式渲染
- [ ] 多模型并行对照
- [ ] 富消息渲染（Markdown/代码/LaTeX/Mermaid）
- [ ] 消息编辑（停止/重生成/分支）
- **交付**：能聊天、能对照

### Phase 2：助手与知识库（3–4 周）
- [ ] 助手系统（内置 8 个业务助手 + 平台管家特殊助手框架）
- [ ] 按助手归集会话
- [ ] 知识库：文档解析 + chunk + embedding + 向量检索
- [ ] RAG 注入
- [ ] 知识库检索测试页
- [ ] **平台管家：模型推荐（`model.local.recommend`）+ 已装模型列表**
- **交付**：助手市场可用，知识库 RAG 可用

### Phase 3：Agent 与 MCP（3–4 周）
- [ ] MCP Server 管理（Node 系 stdio）
- [ ] 工具 schema 注入与调用
- [ ] Work Agent（联网/文件/日历/抓取工具）
- [ ] 多步推理循环
- [ ] **平台管家：故障诊断（`system.diagnose`）—— DB/Provider/MCP/路径/向量一致性**
- **交付**：Agent 可用，MCP 可扩展

### Phase 4：加密与便携强化（2–3 周）
- [ ] 三层加密实现与切换
- [ ] 主密码 / 无密码模式
- [ ] WebDAV 备份（手动 + 定时）
- [ ] 多运行时打包脚本
- [ ] **平台管家：安全检测（`security.audit`）+ 垃圾清理（`system.cleanup`）+ 修复（`system.repair`）**
- [ ] **买断制授权：RSA 公钥内置 + `license.lic` 离线验签 + 功能级解锁**
- [ ] **隐私锁核心：锁屏状态机 + 主进程 Store 多窗口同步 + 后台任务保活**
- **交付**：加密可用，授权可验签，可打包成便携文件夹

### Phase 5：体验与定制（2–3 周）
- [ ] 快捷问答浮窗（全局快捷键）
- [ ] 选区助手浮条
- [ ] 主题/透明/Custom CSS
- [ ] 侧栏显隐排序
- [ ] 多模型头像/品牌色
- [ ] **平台管家 UI：健康中心页 + 首次启动体检引导 + 换电脑检测提示 + 模型下载进度**
- [ ] **隐私锁 UI：锁屏遮罩 + 手动/自动/系统联动触发 + 解锁面板**
- [ ] **更新机制：`app.asar` 增量更新 + 签名校验 + 失败回滚 + 更新源配置**
- **交付**：v1.0 正式版

**v1 总工期估算：15–20 周（单人全职）；2–3 人团队可压缩至 10–12 周。**（平台管家、授权、隐私锁、更新机制合计约 +2 周）

---

## 十六、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| **单包跨 OS 体积大**（~1GB） | 用户下载/拷贝慢 | 提供「全平台包」与「单平台包」两种下载；全平台包为便携默认 |
| **原生模块跨平台编译** | 构建复杂 | 用 `electron-rebuild` + `prebuild-install`，CI 矩阵构建四平台 |
| **SQLCipher 预编译** | 各平台版本 | `@journeyapps/sqlcipher` 已提供多平台预编译 |
| **MCP Python 系不可用** | 部分 Server 不能跑 | v1 明确仅 Node 系；v2 捆绑便携 Python |
| **主密码遗忘 = 数据永久丢失** | 用户体验灾难 | 明确提示；可选「密码提示」；可选「可信设备导出恢复密钥」 |
| **移动硬盘读写速度慢** | 大知识库/向量卡 | 向量索引做内存缓存；大文件懒加载；提示用 USB 3.0+ |
| **加密后性能损耗** | 数据库变慢 | SQLCipher 性能损耗约 10–25%，对桌面对话可接受 |
| **跨平台路径差异** | Windows 盘符变化 | 全用 `path.join` + 应用目录锚定，不写死盘符 |
| **多窗口内存膨胀** | 休眠标签仍占内存 | 实现 dormant 状态，超阈值释放渲染层 |
| **安全：renderer 注入** | XSS → 数据泄露 | 严格 CSP + contextIsolation + 白名单 IPC + 内容转义 |
| **授权被破解** | 付费功能被盗用 | 五层防破解（签名/混淆/原生校验/时间防回拨/联网校验），目标是破解成本 > 购买成本 |
| **锁屏后任务异常** | 用户以为在跑实际断了 | 锁屏仅隐藏 UI，主进程任务不受影响；锁屏状态下任务进度可在解锁面板显示 |
| **增量更新失败** | App 无法启动 | 更新前备份 `app.asar`，验签失败/替换失败自动回滚到上一版本 |
| **授权文件丢失** | 用户付费后无法使用 | 支持重新下载授权（绑定邮箱）；授权文件可导出备份 |

---

## 十七、成功指标（v1）
- ✅ 一个文件夹拷贝到任意平台机器，双击即可运行，数据完整
- ✅ 支持 ≥ 4 类 Provider（OpenAI 兼容 / Gemini / Anthropic / Ollama）
- ✅ 一问多答 ≥ 3 模型并发对照
- ✅ 8 个业务助手 + 1 个平台管家可用
- ✅ 知识库支持 PDF/DOCX/XLSX/URL，RAG 检索可演示
- ✅ MCP Node 系 Server 可加载并调用
- ✅ 三层加密可切换，主密码保护数据
- ✅ **隐私锁：手动/自动/系统联动锁屏可用，锁屏后后台任务不中断**
- ✅ WebDAV 备份可手动触发
- ✅ 全局快捷键唤起快捷问答
- ✅ **平台管家：硬件画像可生成、模型推荐可给出、一键体检/清理/诊断可运行、换电脑可检测并提示**
- ✅ **买断制授权文件可离线验签、功能级解锁生效**
- ✅ **`app.asar` 增量更新可手动触发、签名校验通过、失败可回滚**

---

## 十八、不做清单（v1 明确排除）
- ❌ 终端命令执行 / 代码沙箱
- ❌ Python 系 MCP Server
- ❌ 绘图模块
- ❌ 独立翻译模块（助手级翻译即可）
- ❌ 迷你应用（Mini Program）
- ❌ 独立笔记模块
- ❌ 独立文件管理模块（用系统文件选择器）
- ❌ Channels（飞书/TG/Discord 机器人）
- ❌ 账号系统 / 云端同步（仅 WebDAV 备份）

> 这些延后不是不重要，而是避免 v1 范围爆炸。核心先把「便携 + 多模型对话 + 助手 + 知识库 + Agent + MCP + 加密」做扎实。

---

## 十九、给你的三条关键建议

1. **接受「多运行时同包」而非追求单二进制**。这是物理限制，不是妥协。用户体验上「一个文件夹随身带」已经满足便携诉求，且工程上完全可控。

2. **v1 务必砍掉终端/沙箱**。你已主动提出，非常正确。终端执行 + 沙箱是安全合规的无底洞，会拖垮整个 v1 节奏。先用轻量工具（联网/文件/日历）把 Agent 跑通，v2 再考虑受限沙箱。

3. **加密从第一天就内建，不要后补**。加密如果作为后加功能，数据模型和 IPC 都要大改。在 Phase 0/1 就把 SQLCipher 和密钥派生打通，后续功能天然跑在加密层上，成本最低。

---

*文档结束。下一步建议：确认 Phase 0 脚手架方案，可立即开始搭建可在多平台启动的便携空壳。*
