# PocketAI 模块详细设计

> 配套文档：[产品策划书.md](./产品策划书.md)
> 目标：把每个模块细化到「可直接开工实现」的程度——接口定义、数据结构、关键流程、实现要点。

---

## L1 本地数据层

### M1.1 数据库与迁移

**目标**：提供统一的加密 SQLite 访问层，支持版本化迁移。

**核心功能**
- SQLCipher 加密连接（主密码派生密钥）
- 数据库版本迁移（`migrations/` 目录，`schema_migrations` 表记录已执行版本）
- 连接池（better-sqlite3 同步 API，单连接即可，用 WAL 模式提升并发）
- 事务封装（批量写入用事务）

**接口设计**
```ts
// main/db/database.ts
class DatabaseService {
  constructor(masterKey: Buffer)          // 用派生密钥打开加密库
  getHandle(): Database                    // 返回 better-sqlite3 句柄
  migrate(): void                          // 执行未跑的迁移
  transaction<T>(fn: (db: Database) => T): T
  close(): void
  integrityCheck(): { ok: boolean; details: string }  // 平台管家诊断用
}
```

**迁移约定**
```
migrations/
├── 001_init.sql          -- 核心表（assistants/conversations/messages/providers）
├── 002_knowledge.sql     -- 知识库表
├── 003_mcp.sql           -- MCP 表
├── 004_license.sql       -- 授权记录表
└── ...
```
每个迁移文件是一个事务，失败自动回滚；版本号单调递增。

**实现要点**
- WAL 模式：`PRAGMA journal_mode=WAL`，读写不互斥
- 外键开启：`PRAGMA foreign_keys=ON`
- 加密：打开时 `PRAGMA key = 'x\'' + hex(key) + '\''`
- 所有时间戳用 Unix 毫秒整数（`INTEGER`），避免时区问题

---

### M1.2 向量索引

**目标**：基于 sqlite-vec 的本地向量检索，零外部依赖。

**核心功能**
- 向量写入/更新/删除
- Top-K 近似检索（cosine / L2）
- 向量维度可配（跟随 embedding 模型）
- 索引重建（删除文档后清理孤儿 chunk）

**接口设计**
```ts
// main/knowledge/vector-store.ts
class VectorStore {
  constructor(db: Database)
  addChunk(chunkId: string, docId: string, kbId: string, content: string, embedding: number[]): void
  removeByDoc(docId: string): void
  removeByKb(kbId: string): void
  search(kbId: string, queryEmbedding: number[], topK: number): ChunkHit[]
  rebuildIndex(): { removed: number }   // 清理孤儿，平台管家用
}
```

**表结构**
```sql
CREATE VIRTUAL TABLE kb_vec USING vec0(
  chunk_id TEXT PRIMARY KEY,
  embedding FLOAT[1536]   -- 维度由 embedding 模型决定
);
CREATE TABLE kb_chunks (
  id TEXT PRIMARY KEY,
  doc_id TEXT, kb_id TEXT,
  content TEXT,
  token_count INTEGER,
  created_at INTEGER
);
```

**实现要点**
- sqlite-vec 作为 SQLite 扩展加载（`.loadExtension`），各平台 `.dll/.so/.dylib` 随包
- embedding 维度必须统一：同一知识库用同一 embedding 模型，换模型需重建
- 检索后用 chunk_id join `kb_chunks` 取正文
- 内存缓存：热点知识库的向量加载到内存，减少硬盘 IO（便携模式自动开启）

---

### M1.3 文档解析

**目标**：把多种格式文档转为纯文本 + 分块。

**支持格式**：PDF / DOCX / PPTX / XLSX / HTML / URL / Sitemap / 纯文本 / Markdown

**接口设计**
```ts
// main/knowledge/parsers/index.ts
interface ParseResult {
  text: string
  metadata: { source: string; pageCount?: number; ... }
}

interface Parser {
  supports(type: string): boolean
  parse(filePath: string): Promise<ParseResult>
}
```

**解析器选型**
| 格式 | 库 | 说明 |
|---|---|---|
| PDF | `pdf-parse` | 纯 JS，提取文本 |
| DOCX | `mammoth` | 转 HTML 再提取文本 |
| PPTX | `officeparser` 或 `pptx2json` | 提取备注+文本 |
| XLSX | `xlsx` (SheetJS) | 每个 sheet 转 CSV 文本 |
| HTML/URL | `cheerio` + `readability` | 去标签、提取正文 |
| Sitemap | `sitemapper` | 抓取 URL 列表 → 逐个解析 HTML |

**分块策略**
```ts
// main/knowledge/chunker.ts
function chunkText(text: string, opts: {
  chunkSize: number      // 默认 800 tokens
  chunkOverlap: number   // 默认 200 tokens
}): Chunk[]
```
- 按 token 数分块（用 `tiktoken` 或简单字符估算）
- 重叠保证上下文连续
- 保留 chunk 顺序（`sequence` 字段）

**实现要点**
- 解析在主进程 Worker 线程跑，不阻塞 UI
- 大文件分批解析，可中断
- 解析失败记录错误，不中断整个知识库入库
- URL 解析遵守 `robots.txt`（可选），加请求间隔避免被封

---

### M1.4 WebDAV 备份

**目标**：手动/定时把 `data/` 备份到 WebDAV 远端。

**接口设计**
```ts
// main/backup/webdav.ts
class WebDAVBackup {
  constructor(config: { url: string; username: string; password: string })
  testConnection(): Promise<boolean>
  backupNow(): Promise<BackupResult>   // 打包 data/ 上传
  restore(remotePath: string): Promise<void>
  schedule(cronExpr: string): void     // 定时备份
}
```

**备份策略**
- 全量备份：打包 `data/`（排除 `data/logs/`）为 `.zip`，文件名带时间戳
- 增量备份（v2）：只传变化的文件
- 加密备份：备份包可用主密码加密后再上传
- 保留策略：远端保留最近 N 份（默认 5 份），自动清理旧的

**实现要点**
- 用 `webdav` 库
- 备份前 flush WAL，确保数据一致性
- 大备份分片上传，支持断点续传
- 备份进度通过 IPC 推送到 UI

---

## L2 模型抽象层

### M2.1 Provider 抽象与适配器

**目标**：统一所有模型供应商的接口，上层零感知差异。

**统一接口**
```ts
// main/providers/types.ts
interface ChatMessage {
  role: 'system' | 'user' | 'assistant' | 'tool'
  content: string
  tool_calls?: ToolCall[]
  tool_call_id?: string
}

interface ChatOptions {
  model: string
  temperature?: number
  max_tokens?: number
  top_p?: number
  stop?: string[]
  tools?: ToolSchema[]
  stream?: boolean
}

interface ChatCompletionResult {
  content: string
  tool_calls?: ToolCall[]
  usage: { prompt_tokens: number; completion_tokens: number }
  finish_reason: string
}

interface ProviderAdapter {
  readonly type: ProviderType        // openai/gemini/anthropic/azure/ollama/...
  chat(messages: ChatMessage[], opts: ChatOptions): Promise<ChatCompletionResult>
  streamChat(messages: ChatMessage[], opts: ChatOptions): AsyncIterable<StreamChunk>
  listModels(): Promise<ModelInfo[]>
}
```

**适配器实现**
```
main/providers/
├── base.ts              -- 适配器基类（统一错误处理、超时、重试）
├── openai.ts            -- OpenAI + 任意 OpenAI-compatible
├── gemini.ts            -- Google Gemini
├── anthropic.ts         -- Anthropic Claude
├── azure.ts             -- Azure OpenAI（继承 openai，改 base_url 和认证头）
└── ollama.ts            -- Ollama 本地
```

**归一化处理**
- **流式**：所有适配器的 SSE / fetch stream 统一转为 `AsyncIterable<StreamChunk>`
- **工具调用**：各厂商 tool_calls 格式不同，统一转为 `{ name, arguments }`
- **错误码**：HTTP 401/429/500 统一映射为 `ProviderError`（带 `retryable` 标志）
- **多 Key 轮询**：基类内置轮询器，一个 Key 失败自动切下一个

**头像/品牌色映射**
```ts
const PROVIDER_BRAND = {
  openai:     { name: 'OpenAI',     color: '#10a37f', avatar: '🤖' },
  gemini:     { name: 'Gemini',     color: '#4285f4', avatar: '✨' },
  anthropic:  { name: 'Anthropic',  color: '#d97757', avatar: '🧠' },
  ollama:     { name: 'Ollama',     color: '#000000', avatar: '🦙' },
  ...
}
```

**实现要点**
- 适配器用 `fetch`（Node 18+ 内置），不引入 axios，减少依赖
- 超时默认 60s，流式无超时（或 10 分钟）
- 模型列表缓存到 `providers.models` 字段，TTL 24h
- Ollama/LM Studio 的 base_url 本地默认 `http://localhost:11434`

---

### M2.2 多模型并发调度

**目标**：同一段输入并发发往多个模型，流式合并渲染。

**接口设计**
```ts
// main/chat/multi-model-runner.ts
class MultiModelRunner {
  run(params: {
    input: ChatMessage[]
    models: { providerId: string; model: string }[]
    options: ChatOptions
    onChunk: (modelId: string, chunk: StreamChunk) => void
    onError: (modelId: string, error: Error) => void
    onDone: (modelId: string, result: ChatCompletionResult) => void
  }): Promise<void>
  abort(): void
}
```

**并发策略**
- `Promise.allSettled` 并发，单个模型失败不影响其他
- 限流：同一 Provider 最多 N 个并发（避免触发 429）
- 流式输出通过 IPC 按 `modelId` 分发到对应列

**对照视图数据结构**
```ts
interface ComparisonState {
  conversationId: string
  userMessageId: string
  responses: Map<modelId, {
    status: 'pending' | 'streaming' | 'done' | 'error'
    content: string
    error?: string
  }>
}
```

**实现要点**
- 每个模型的请求独立 AbortController，可单独停止
- 流式 chunk 带序号，前端按序拼接，防乱序
- 全部完成/失败后统一写入数据库（每条 AI 消息关联 modelId）

---

## L3 上下文层

### M3.1 助手系统

**目标**：管理助手的定义、挂载、会话归集。

**助手数据结构**
```ts
interface Assistant {
  id: string
  name: string
  description: string
  avatar: string              // emoji 或图片路径
  systemPrompt: string         // 支持变量占位 {{user_name}} {{date}}
  defaultProviderId?: string
  defaultModel?: string
  defaultParams?: ModelParams
  toolPermissions: string[]    // 允许使用的工具 ID 列表
  skillIds: string[]           // 挂载的 Skill
  welcomeMessage?: string
  isBuiltin: boolean
  isPinned: boolean
  createdAt: number
}
```

**变量占位**
```
{{user_name}}    → 当前用户名
{{date}}         → 当前日期
{{time}}         → 当前时间
{{weekday}}      → 星期
{{knowledge}}    → RAG 检索结果（由 RAG 模块注入）
{{tools}}        → 工具描述（由工具模块注入）
{{memory}}       → 用户级记忆
```
SystemPrompt 在发送前做一次模板渲染，把占位符替换为实际值。

**会话归集**
- `conversations.assistant_id` 外键关联
- 助手列表页显示该助手的最近 N 条会话
- 新建会话自动归属到当前激活的助手

**实现要点**
- 内置助手存 `extensions/assistants/`（JSON 文件），启动时导入 DB
- 用户可复制内置助手为「我的助手」后编辑
- 助手导入导出：JSON 文件，便于分享

---

### M3.2 知识库 RAG

**目标**：检索 → 重排 → 拼装到 context。

**RAG 流程**
```
用户问题
  ↓
1. Query 改写（可选，用 LLM 把问题改写成更适合检索的形式）
  ↓
2. 向量化（用知识库的 embedding 模型）
  ↓
3. 向量检索 Top-K（默认 K=20）
  ↓
4. 重排（用 reranker 模型或简单 BM25 加权，取 Top-N，默认 N=5）
  ↓
5. 拼装 context：
   ```
   以下是相关知识库内容：
   [文档1标题] chunk1内容...
   [文档1标题] chunk2内容...
   [文档2标题] chunk3内容...
   ```
  ↓
6. 注入到 messages 的 system 或 user 消息末尾
```

**接口设计**
```ts
// main/knowledge/rag.ts
class RAGService {
  async retrieve(kbIds: string[], query: string, opts: {
    topK?: number          // 检索数，默认 20
    topN?: number          // 重排后数，默认 5
    rerank?: boolean
  }): Promise<RetrievedChunk[]>
  buildContext(chunks: RetrievedChunk[]): string
}
```

**实现要点**
- v1 重排用简单策略（向量分数 + BM25 加权），不引入额外 reranker 模型
- Query 改写可选，默认关（省一次 LLM 调用）
- 检索结果带来源（文档名 + 页码），渲染时可显示引用
- 长 context 截断：超过模型 context 窗口时，按相关性保留

---

### M3.3 工具注入

**目标**：把可用工具的 schema 注入到请求，处理工具调用循环。

**工具统一 schema**
```ts
interface ToolSchema {
  id: string                 // 唯一标识，如 'web.fetch'
  name: string               // 给 LLM 看的函数名
  description: string
  parameters: JSONSchema      // 函数参数
  source: 'builtin' | 'mcp'  // 来源
  permission?: 'auto' | 'confirm' | 'deny'
}
```

**工具调用循环（ReAct）**
```
1. 发送 messages + tools 给 LLM
2. 若返回 tool_calls：
   a. 逐个执行工具（内置工具直接调，MCP 工具转发）
   b. 把工具结果作为 role=tool 消息追加
   c. 回到步骤 1（最多 N 轮，默认 10）
3. 若返回最终内容，结束
```

**工具执行权限**
- `auto`：自动执行（无副作用工具，如 web.fetch、计算器）
- `confirm`：弹确认框让用户同意（如写文件、发消息）
- `deny`：禁止

**实现要点**
- 工具调用循环在主进程跑，不阻塞渲染
- 每轮 tool call 都写入消息记录（role=tool_call + role=tool_result），可追溯
- 超时控制：单个工具执行超时 30s，整体循环超时 5 分钟

---

### M3.4 记忆与变量

**目标**：跨会话保留用户偏好与关键信息。

**记忆层级**
| 层级 | 范围 | 存储 | 用途 |
|---|---|---|---|
| 用户级 | 所有会话 | `user_memory` 表 | 用户名、偏好、常用语言 |
| 助手级 | 某助手的所有会话 | `assistant_memory` 表 | 该助手特有的上下文 |
| 会话级 | 单个会话 | messages 表 | 对话历史本身 |

**记忆提取**
- 会话结束或用户标记时，用 LLM 提取关键信息存入记忆
- 记忆条目：`{ key, value, importance, expires_at? }`
- 注入时取 importance 高的、未过期的

**实现要点**
- v1 记忆功能简化：仅用户级记忆，手动添加 + 自动提取（v2）
- 记忆注入有上限（避免 context 爆炸），默认最多 10 条

---

## L4 能力插件层

### M4.1 MCP 管理

**目标**：安装、启停、管理 Node 系 MCP Server，转发工具调用。

**MCP Server 数据结构**
```ts
interface MCPServer {
  id: string
  name: string
  transport: 'stdio' | 'http'
  // stdio
  command?: string            // node
  args?: string[]             // ['-e', '...'] 或脚本路径
  env?: Record<string, string>
  // http
  url?: string
  enabled: boolean
  status: 'stopped' | 'starting' | 'running' | 'error'
  tools?: ToolSchema[]        // 连接后拉取
  lastError?: string
}
```

**生命周期管理**
```ts
// main/mcp/mcp-manager.ts
class MCPManager {
  install(server: MCPServer): Promise<void>     // 校验配置
  start(id: string): Promise<void>              // spawn 进程，发起 initialize 握手
  stop(id: string): void                         // 优雅关闭进程
  listTools(id: string): Promise<ToolSchema[]>   // tools/list
  callTool(id: string, name: string, args: any): Promise<any>
  restart(id: string): Promise<void>
}
```

**stdio 传输**
- 用 Electron 内置 Node 直接 `child_process.spawn('node', args)`
- 走 JSON-RPC 2.0 over stdio
- 进程崩溃自动重启（最多 3 次，然后标记 error）

**实现要点**
- MCP Server 进程的 cwd 设为 `extensions/mcp/{serverId}/`
- stdout 解析 JSON-RPC，stderr 作为日志
- 工具调用超时 30s
- 平台管家诊断时检查所有 enabled server 的状态

---

### M4.2 Skills

**目标**：可复用能力包，挂载到助手。

**Skill 包结构**
```
extensions/skills/skill-sql-optimizer/
├── skill.json        -- 元数据
├── prompt.md         -- 提示模板
├── tools.json        -- 需要的工具权限声明
└── examples/         -- 示例
```

**skill.json**
```json
{
  "id": "sql-optimizer",
  "name": "SQL 优化器",
  "description": "分析慢查询并给出优化建议",
  "version": "1.0.0",
  "required_tools": ["web.fetch", "file.read"],
  "prompt_file": "prompt.md"
}
```

**挂载机制**
- 助手级：助手详情页勾选 Skill，该助手所有会话自动加载 Skill 的 prompt
- 会话级：输入框 `@skill` 临时挂载，仅当前轮生效

**注入方式**：Skill 的 `prompt.md` 内容追加到 SystemPrompt 末尾，作为能力增强。

**实现要点**
- Skill 是纯数据（prompt + 工具声明），不含可执行代码，天然安全
- 工具权限交集：助手允许的工具 ∩ Skill 声明的工具 = 实际可用工具
- v1 内置 6–10 个 Skill，不做市场

---

### M4.3 Work Agent 引擎

**目标**：带工具权限 + 多步推理的 Agent。

**与普通助手的区别**
| | 普通助手 | Work Agent |
|---|---|---|
| 工具 | 可选挂载 | 默认带工具权限 |
| 推理 | 单轮 | 多步 ReAct 循环 |
| 典型场景 | 问答、创作 | 查资料、处理文件、多步任务 |

**Agent 循环**
```
用户输入
  ↓
循环（最多 N 步）：
  1. LLM 决定：调用工具 or 给出最终回答
  2. 若调工具 → 执行 → 结果回填 → 继续
  3. 若最终回答 → 结束
  ↓
输出最终回答（带中间步骤展示）
```

**Agent 配置**
```ts
interface AgentConfig {
  maxSteps: number           // 最大推理步数，默认 10
  tools: string[]            // 可用工具 ID
  systemPrompt: string
  thinkingMode: 'hidden' | 'visible'   // 是否展示中间推理
}
```

**v1 内置工具集**（无副作用）
| 工具 | 说明 |
|---|---|
| `web.fetch` | 抓取 URL 正文（readability 提取） |
| `web.search` | 联网搜索（需配置搜索 API） |
| `file.read` | 读取授权目录下的文件 |
| `file.write` | 写入文件（需用户确认） |
| `calendar.read` | 读取本地 .ics / 飞书日历 |
| `calculator` | 数学计算 |
| `time.now` | 当前时间 |

**实现要点**
- 中间步骤（thought + action + observation）可折叠展示
- 每步可中断、可回退
- 工具执行结果要截断（避免超长 observation 撑爆 context）

---

## L5 表现层

### M5.1 三层导航

**Sidebar（左侧栏）**
- 固定入口：对话、Agent、知识库、文件、平台管家、设置
- 可折叠为图标栏、可悬浮（拖拽脱离）、宽度持久化到 config
- 菜单项支持用户显隐与排序（设置里配置）
- 底部：当前模型/Provider 状态、加密锁状态、授权状态

**Tab Bar（顶部标签）**
- 浏览器式标签，支持新建、关闭、拖拽排序、固定
- 标签状态：active / pinned / dormant / detached
- 超 N 个标签自动休眠（释放渲染层，保留滚动位置）
- 右键菜单：固定/休眠/剥离为独立窗口/关闭其他/关闭右侧

**浮窗层**
- 快捷问答浮窗（全局快捷键唤起，独立 BrowserWindow，置顶）
- 选区助手浮条（划词后屏幕坐标定位的小浮条）
- Detached tab（从标签剥离的独立窗口）

**实现要点**
- 侧栏状态、标签状态持久化到 `config.json`
- 多窗口通过 IPC 同步标签状态（主进程为真源）
- 快捷键：`Ctrl/Cmd+K` 快捷问答，`Ctrl/Cmd+L` 锁屏，`Ctrl/Cmd+T` 新标签

---

### M5.2 对话主区

**布局**
- 左侧：消息流（用户消息靠右、AI 消息靠左）
- 右侧（对照模式）：多模型回答并排
- 底部：多模态输入框

**消息渲染**
- Markdown（react-markdown）
- 代码高亮（rehype-highlight，支持复制按钮）
- LaTeX（remark-math + rehype-katex）
- Mermaid（代码块 ```mermaid 渲染为图）
- HTML 预览（沙箱 iframe，防 XSS）
- 引用切片（RAG 来源可展开）

**输入框**
- 附件（图片/文件，拖入或点击）
- `@知识库`：选择注入哪个知识库
- `@工具`：临时启用某个工具
- `@模型`：临时切换/追加模型对照
- 参数覆盖：温度、max_tokens 等临时改
- 临时 system：输入框上方可展开编辑本轮 system prompt
- 快捷键：`Enter` 发送，`Shift+Enter` 换行，`Esc` 停止生成

**消息操作**
- 停止生成、重生成、编辑（改用户消息重跑）、分支（从某条消息开新分支）、复制、删除
- 分支用 `messages.parent_id` 实现，形成树结构

**实现要点**
- 流式渲染用虚拟列表（长会话不卡）
- 代码块用 `react-syntax-highlighter` 或 shiki
- HTML 预览用 `<iframe sandbox>` 隔离

---

### M5.3 助手市场

**页面结构**
- 顶部：分类标签（全部/业务助手/平台管家/已安装）
- 网格/列表：助手卡片（头像、名称、描述、标签）
- 详情页：SystemPrompt 预览、默认模型/参数、工具权限、挂载的 Skill
- 操作：安装/卸载、置顶、复制为我的助手、导出

**内置助手加载**
- 启动时扫描 `extensions/assistants/*.json`
- 与 DB 中已安装的合并（内置的标记 `isBuiltin=true`）
- 内置助手不可删除，但可禁用

---

### M5.4 知识库 UI

**列表页**
- 知识库卡片（名称、文档数、chunk 数、大小、更新时间）
- 操作：新建、删除、重命名、配置（embedding 模型、chunk 参数）

**知识库详情页**
- 文档列表（来源、类型、chunk 数、状态）
- 操作：添加文档（文件/URL/Sitemap/手动录入）、删除、重新解析
- **检索测试**：输入问题 → 显示召回的 chunk + 相似度分数
- **chunk 预览**：查看文档分块效果

**入库流程 UI**
- 添加文档 → 解析进度 → 分块 → 向量化 → 完成
- 失败的文档可单独重试

---

### M5.5 设置与定制

**设置分组**
| 分组 | 内容 |
|---|---|
| 模型 | Provider 增删改、Key 管理、模型列表刷新、默认模型 |
| 助手 | 助手管理、Skill 挂载 |
| 知识库 | 知识库配置、embedding 模型 |
| MCP | Server 增删、启停、日志 |
| 通用 | 主题（明/暗/跟随/透明）、语言、默认参数 |
| 外观 | Custom CSS、侧栏排序、布局（气泡/列表）、字体 |
| 安全 | 加密级别、主密码修改、锁屏设置、剪贴板清除 |
| 备份 | WebDAV 配置、立即备份、恢复、定时备份 |
| 授权 | 导入授权、查看授权信息、更新续费 |
| 关于 | 版本、检查更新、日志导出 |

**Custom CSS**
- 设置页提供代码编辑器
- 注入到 renderer 的 `<style>`
- 提供预设主题包（可导入导出）

---

### M5.6 快捷问答与选区助手

**快捷问答浮窗**
- 全局快捷键 `Ctrl/Cmd+K` 唤起
- 置顶小窗，输入即问，可选择模型/助手
- 回答可一键插入到活动窗口（剪贴板）
- 不打开主窗口也能用

**选区助手浮条**
- 监听全局划词（选中文字后），在选区旁显示小浮条
- 预设操作：翻译、总结、改写、解释、续写
- 点击后唤起浮窗执行

**实现要点**
- 全局快捷键用 `electron.globalShortcut`
- 划词检测用 `iohook`（原生模块，需多平台编译）或剪贴板监听
- 浮窗是独立 BrowserWindow，`alwaysOnTop: true`

---

## 横切模块

### M6.1 加密与隐私锁

（详见策划书第十章，此处补充实现细节）

**加密服务**
```ts
// main/crypto/crypto-service.ts
class CryptoService {
  private masterKey: Buffer | null = null

  setMasterPassword(password: string, salt: Buffer): void   // 派生 masterKey
  getSqlCipherKey(): string                                  // hex 格式
  getFieldKey(): Buffer
  encryptField(plaintext: string): string                    // AES-256-GCM → base64(iv+tag+cipher)
  decryptField(ciphertext: string): string
  clearMasterKey(): void                                     // 锁屏时调用
  isUnlocked(): boolean
}
```

**隐私锁**
```ts
// main/lock/lock-service.ts
class LockService {
  private locked = false
  lock(): void                   // 锁屏
  unlock(password: string): boolean
  isLocked(): boolean
  onAutoLock(callback: () => void)  // 空闲超时自动锁
  bindSystemLock(): void         // 监听系统锁屏事件
}
```
- 锁屏时 `masterKey` 保留（保证后台任务继续），仅 UI 遮罩
- 若用户选「锁屏清除密钥」模式（更安全），则锁屏时清密钥，后台加密任务暂停

---

### M6.2 平台管家

（详见策划书 6.4，此处补充工具实现）

**系统工具集**
```ts
// main/steward/tools.ts
// 全部为内置工具，不经 MCP

interface HardwareInfo {
  cpu: { model: string; cores: number }
  memory: { total: number; free: number }   // bytes
  gpu: { name: string; memory?: number; cuda: boolean; mps: boolean }[]
  disk: { type: 'ssd' | 'hdd' | 'usb'; removable: boolean }
  os: { platform: string; release: string; arch: string }
}

function system_hardware_info(): HardwareInfo
function model_local_list(): { name: string; size: string; modified: number }[]
function model_local_recommend(hardware: HardwareInfo): ModelRecommendation[]
function model_local_pull(model: string): AsyncIterable<{ progress: number }>
function security_audit(): SecurityReport
function system_cleanup(level: 'safe' | 'deep'): CleanupResult
function system_diagnose(): DiagnosisResult
function system_repair(issueId: string): RepairResult
```

**硬件检测实现**
- CPU/内存/OS：Node `os` 模块
- GPU：Windows 用 `wmic` / `dxdiag`，macOS 用 `system_profiler`，Linux 用 `lspci`
- 磁盘类型：Windows `wmic diskdrive`，macOS `diskutil`，Linux `/sys/block`

**模型推荐规则**
```
内存 < 8GB   → 不推荐本地大模型，建议用云端 API
内存 8-16GB  → 推荐 7B Q4 模型
内存 16-32GB → 推荐 13B Q4 / 7B Q8
内存 > 32GB  → 推荐 34B Q4 / 13B Q8
有独显(CUDA/MPS) → 推荐 GPU 加速版本
```

**故障诊断项**
| 诊断项 | 检查方法 | 修复 |
|---|---|---|
| DB 完整性 | `PRAGMA integrity_check` | 重建索引 |
| Provider 连通 | 逐个发测试请求 | 提示检查 Key/网络 |
| MCP 健康 | spawn 测试 + 握手 | 重启 Server |
| 路径锚定 | 检查 data/ 在 app 同级 | 自动迁移 |
| 向量一致性 | chunk 数 vs 文档声明数 | 重建向量索引 |

---

### M6.3 商用授权

（详见策划书第十三章，此处补充实现）

**授权服务**
```ts
// main/license/license-service.ts
class LicenseService {
  private publicKey: string          // 内置 RSA 公钥
  loadLicense(): LicenseInfo | null   // 读 data/license.lic 验签
  importLicense(fileContent: string): boolean
  hasFeature(feature: string): boolean
  getPlan(): 'free' | 'pro' | 'team'
  isUpdateExpired(): boolean          // update_until 是否已过
}
```

**功能级解锁**
```ts
const FEATURE_GATES = {
  'knowledge_base': { plans: ['pro', 'team'] },
  'mcp':            { plans: ['pro', 'team'] },
  'multi_model':    { plans: ['pro', 'team'] },
  'platform_steward': { plans: ['pro', 'team'] },
  'webdav_backup':  { plans: ['pro', 'team'] },
  'unlimited_assistants': { plans: ['pro', 'team'] },
}
```
免费版硬限制：2 助手、2 Provider、无知识库/MCP、单模型对话。

**签发脚本（服务端）**
```ts
// scripts/sign-license.ts
import { createSign } from 'node:crypto'
const payload = { licensee, plan, issued_at, expires_at: 0, update_until, max_devices, features }
const sign = createSign('RSA-SHA256')
sign.update(JSON.stringify(payload))
const signature = sign.sign(privateKey, 'base64')
const license = { ...payload, signature }
// 输出 license.lic
```

---

### M6.4 更新机制

（详见策划书第十四章，此处补充实现）

**更新服务**
```ts
// main/updater/update-service.ts
class UpdateService {
  checkForUpdates(): Promise<UpdateInfo | null>   // 查更新源版本
  downloadUpdate(version: string): Promise<string> // 下载 app.asar
  applyUpdate(): Promise<void>                     // 原子替换 + 重启
  rollback(): void
  getUpdateSource(): string
  setUpdateSource(url: string): void
}
```

**增量更新流程**
```
1. 检查更新源的 latest.json → { version, app_asar_url, signature }
2. 下载 app.asar 到 data/update/app.asar.new
3. 验签（RSA-SHA256）
4. 备份当前 app.asar → app.asar.bak
5. 原子替换：rename app.asar.new → app.asar
6. 提示重启
7. 重启后：若启动失败，自动用 app.asar.bak 回滚
```

**版本文件 `latest.json`**
```json
{
  "version": "1.2.0",
  "app_asar_url": "https://cdn.example.com/1.2.0/app.asar",
  "signature": "base64...",
  "min_runtime_version": "1.0.0",   // 需要的最低运行时版本
  "release_notes": "..."
}
```
若 `min_runtime_version` > 当前运行时版本，提示需下载全量包。

---

### M6.5 便携启动

**启动器职责**
- 检测平台（process.platform）
- 确保 `data/` 等目录存在
- 加载 config.json
- 初始化加密（若加密则要求输入主密码）
- 加载授权
- 创建主窗口

**路径锚定**（详见策划书 9.1）
- `APP_ROOT` = 可执行文件目录
- 所有数据路径 = `path.join(APP_ROOT, 'data', ...)`
- **禁止**使用 `app.getPath('userData')`

**开发 vs 生产**
```ts
const APP_ROOT = app.isPackaged
  ? path.dirname(app.getPath('exe'))   // 打包后：可执行文件目录
  : process.cwd()                       // 开发时：项目根
```

**多运行时加载**
- 原生模块（better-sqlite3、sqlcipher、sqlite-vec、iohook）按 `platform-arch` 分目录
- 启动时动态 require 对应平台的 `.node`
- 打包脚本用 `electron-rebuild` 为每个平台编译

---

## 模块依赖关系

```
L5 表现层 ──依赖──▶ L4 能力插件层 ──依赖──▶ L3 上下文层 ──依赖──▶ L2 模型抽象层 ──依赖──▶ L1 本地数据层
  │                    │                      │                      │                      │
  └── 横切模块：加密/平台管家/授权/更新/便携 ─────────────────────────────────────────────┘
```

**开发顺序建议**（从底向上）：
1. L1 数据库 + 便携启动 → 2. L2 Provider 抽象 → 3. L5 对话主区 → 4. L3 助手/RAG → 5. L4 MCP/Agent → 6. 横切模块

---

*文档结束。下一步可基于此设计开始 Phase 0 脚手架，或先实现某个具体模块。*
